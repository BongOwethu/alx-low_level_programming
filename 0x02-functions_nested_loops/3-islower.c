#include "main.h"

/**
 * _islower - checks if the character is lowercase
 * @c: a cahracter argument
 * Return: 0 or 1 depending on the condition
 */

int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
