#include "main.h"

/**
 * main - Entry point
 * Description: Check for the lowercase letter
 * character to check the case
 * Return: 0 or 1
 */

int _islower(int c)
{
	return (c >= 97 && c <= 127);
}
